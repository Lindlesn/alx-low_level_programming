#include "main.h"

/**
 * print_rev - prints a string in reverse
 *
 * @s: string to be printed in reverse
 *
 */

void print_rev(char *s)
{
	int i = 0;
	int length;

	for (lenth = 0; s[length] != '\0'; i++)
	{
	}
	for (i = length -1; i >= 0; i--)
	{
		_putchar(s[i]);
	}
	_putchar('\n')
}
